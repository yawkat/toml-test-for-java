name: Run tests

on:
  push:
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v6
        with:
          fetch-depth: 0
          submodules: true
      - name: Install uv
        uses: astral-sh/setup-uv@v6
        with:
          enable-cache: true
          cache-dependency-glob: tools/codegen/uv.lock
      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version-file: tools/codegen/pyproject.toml
      - name: Sync codegen environment
        working-directory: tools/codegen
        run: uv sync --locked
      - name: Setup Java 17
        uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: '17'
          cache: maven
      - name: Build
        run: ./mvnw -B -ntp verify
